<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent" android:paddingLeft="5dp"
    android:paddingRight="5dp"
    android:background="@color/background"
    android:paddingTop="5dp"
    android:paddingBottom="5dp" tools:context=".AddShow">

    <AutoCompleteTextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="50dp"
        android:hint="@string/search_hint"
        android:inputType="text"
        android:imeOptions="actionSearch"
        android:textAppearance="@style/TextAppearance.Medium"
        android:textColorHint="@color/material"
        android:textColor="@color/heading1"
        android:id="@+id/autoCompleteTextView" />

    <TextView
        android:text="No search result. Check spellings or try different words."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textAppearance="@style/TextAppearance.Medium"
        android:id="@+id/noSearchResultText"
        android:visibility="gone"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/list_view_add_show"
        android:divider="@android:color/transparent"
        android:dividerHeight="5dp"
        android:paddingTop="5dp"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:paddingBottom="64dp"
        android:clipToPadding="false"
        />


</LinearLayout>